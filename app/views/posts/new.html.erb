<h1>Create a new Post</h1>

<%= form_with(model: @post, url: new_user_post_path(@user)) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :content %>
  <%= form.text_area :content %>

  <%= form.submit "Create Post" %>
<% end %>
